export const REVIEW_CSS = `
:root{color-scheme:dark;--bg:#071019;--panel:#0d1824;--panel2:#111f2d;--line:rgba(255,255,255,.1);--text:#eef6f5;--muted:#93a6b5;--accent:#7ee0c2;--warn:#e9bd72;--danger:#ff9186}*{box-sizing:border-box}body{margin:0;background:radial-gradient(circle at 10% 0,rgba(68,137,139,.22),transparent 28rem),var(--bg);color:var(--text);font:14px/1.5 Inter,ui-sans-serif,system-ui,-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif}button,input,textarea{font:inherit}button{cursor:pointer}.shell{max-width:1480px;margin:auto;padding:24px}.topbar{display:flex;gap:18px;align-items:center;justify-content:space-between;padding:10px 0 24px;border-bottom:1px solid var(--line)}.brand{display:flex;gap:12px;align-items:center}.mark{display:grid;place-items:center;width:44px;height:44px;border:1px solid rgba(126,224,194,.45);border-radius:12px;color:var(--accent);font-weight:900;font-size:12px}.brand strong{display:block}.brand small,.muted{color:var(--muted)}.operator{text-align:right}.operator strong{display:block}.layout{display:grid;grid-template-columns:360px 1fr;gap:18px;padding-top:18px}.panel{border:1px solid var(--line);background:linear-gradient(180deg,rgba(255,255,255,.045),rgba(255,255,255,.018));border-radius:16px;overflow:hidden}.panel-head{display:flex;align-items:center;justify-content:space-between;gap:12px;padding:16px;border-bottom:1px solid var(--line)}.panel-head h1,.panel-head h2{font-size:16px;margin:0}.btn{border:1px solid var(--line);border-radius:9px;padding:8px 11px;background:rgba(255,255,255,.04);color:var(--text);font-weight:700}.btn-primary{background:var(--text);color:var(--bg)}.btn-danger{border-color:rgba(255,145,134,.35);color:#ffc3bd}.btn:disabled{opacity:.4;cursor:not-allowed}.queue{max-height:calc(100vh - 140px);overflow:auto}.queue-item{display:block;width:100%;text-align:left;padding:14px 16px;border:0;border-bottom:1px solid var(--line);background:transparent;color:var(--text)}.queue-item:hover,.queue-item[data-active="true"]{background:rgba(126,224,194,.06)}.queue-item strong{display:block;margin-bottom:4px}.row{display:flex;gap:8px;align-items:center;justify-content:space-between}.pill{display:inline-flex;border:1px solid var(--line);border-radius:999px;padding:2px 7px;font-size:11px;color:var(--muted)}.pill[data-status="approved_for_processing"],.pill[data-status="processing"]{color:var(--accent);border-color:rgba(126,224,194,.3)}.pill[data-status="rejected"],.pill[data-status="failed"]{color:var(--danger);border-color:rgba(255,145,134,.35)}.detail{padding:18px}.empty{padding:56px;text-align:center;color:var(--muted)}.hero{display:grid;gap:8px;padding-bottom:18px;border-bottom:1px solid var(--line)}.hero h2{font-size:28px;line-height:1.1;margin:0}.eyebrow{color:var(--accent);font-size:11px;text-transform:uppercase;letter-spacing:.14em;font-weight:800}.meta-grid,.control-grid,.evidence-grid{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:10px;margin-top:14px}.card{border:1px solid var(--line);background:rgba(255,255,255,.025);border-radius:12px;padding:12px}.card dt{color:var(--muted);font-size:11px}.card dd{margin:4px 0 0;font-weight:700;overflow-wrap:anywhere}.section{padding:18px 0;border-bottom:1px solid var(--line)}.section:last-child{border-bottom:0}.section h3{margin:0 0 10px;font-size:14px}.section p{color:#c8d3dc;white-space:pre-wrap}.file{display:grid;grid-template-columns:1fr auto;gap:12px;align-items:center;padding:10px 0;border-bottom:1px solid var(--line)}.file:last-child{border-bottom:0}.file small{display:block;color:var(--muted)}.actions{display:flex;gap:8px;flex-wrap:wrap}.decision{display:grid;grid-template-columns:1fr auto auto;gap:8px;margin-top:12px}.decision textarea{min-height:76px;resize:vertical}.field{width:100%;border:1px solid var(--line);border-radius:9px;padding:9px 10px;background:#08121c;color:var(--text);outline:none}.outcome-form{display:grid;gap:8px}.outcome-form .split{display:grid;grid-template-columns:1fr 1fr;gap:8px}.event{display:grid;grid-template-columns:110px 1fr;gap:12px;padding:9px 0;border-bottom:1px solid var(--line)}.event time{color:var(--muted);font-size:11px}.event strong{display:block}.notice{margin-top:12px;padding:10px 12px;border-radius:10px;border:1px solid rgba(233,189,114,.3);background:rgba(233,189,114,.05);color:#f3d9a9}.toast{position:fixed;right:22px;bottom:22px;max-width:420px;padding:12px 14px;border:1px solid var(--line);background:#111d29;border-radius:10px;box-shadow:0 12px 35px rgba(0,0,0,.4)}.toast[data-kind="error"]{border-color:rgba(255,145,134,.4)}[hidden]{display:none!important}@media(max-width:980px){.layout{grid-template-columns:1fr}.queue{max-height:340px}.meta-grid,.control-grid,.evidence-grid{grid-template-columns:1fr 1fr}}@media(max-width:620px){.shell{padding:14px}.meta-grid,.control-grid,.evidence-grid,.decision,.outcome-form .split{grid-template-columns:1fr}.operator{display:none}.hero h2{font-size:22px}}
`;

export const REVIEW_JS = `
const queueEl=document.querySelector('[data-queue]');const detailEl=document.querySelector('[data-detail]');const operatorEl=document.querySelector('[data-operator]');const refreshBtn=document.querySelector('[data-refresh]');const toast=document.querySelector('[data-toast]');let currentId=null;let requests=[];
const esc=(v)=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
function notify(message,kind='ok'){toast.textContent=message;toast.dataset.kind=kind;toast.hidden=false;setTimeout(()=>toast.hidden=true,3600)}
function humanBytes(n){if(n<1024)return n+' B';if(n<1048576)return(n/1024).toFixed(1)+' KB';return(n/1048576).toFixed(1)+' MB'}
async function api(path,init={}){const r=await fetch(path,{...init,headers:{accept:'application/json',...(init.body?{'content-type':'application/json'}:{}),...(init.headers||{})},cache:'no-store'});const b=await r.json().catch(()=>({}));if(!r.ok)throw new Error(b.message||b.error||('HTTP '+r.status));return b}
function queueHtml(item){return '<button class="queue-item" data-id="'+esc(item.requestId)+'" data-active="'+(item.requestId===currentId)+'"><div class="row"><span class="pill" data-status="'+esc(item.status)+'">'+esc(item.status)+'</span><small class="muted">'+esc(new Date(item.updatedAt).toLocaleString())+'</small></div><strong>'+esc(item.title)+'</strong><small class="muted">'+esc(item.serviceType)+' · '+esc(item.requester.organization||item.requester.email)+'</small></button>'}
async function loadQueue(){refreshBtn.disabled=true;try{const b=await api('/api/queue');requests=b.requests||[];operatorEl.textContent=b.operator?.email||'Authenticated operator';queueEl.innerHTML=requests.length?requests.map(queueHtml).join(''):'<div class="empty">No work requests in queue.</div>';queueEl.querySelectorAll('[data-id]').forEach(btn=>btn.addEventListener('click',()=>openRequest(btn.dataset.id)));if(currentId&&!requests.some(x=>x.requestId===currentId)){currentId=null;detailEl.innerHTML='<div class="empty">Select a work request.</div>'}}catch(e){notify(e.message,'error')}finally{refreshBtn.disabled=false}}
function eventHtml(e){return '<div class="event"><time>'+esc(e.at?new Date(e.at).toLocaleString():'')+'</time><div><strong>'+esc(e.title)+'</strong><span class="muted">'+esc(e.detail||'')+'</span></div></div>'}
function detailHtml(r){const canReview=['received_unreviewed'].includes(r.status);const canDecide=['received_unreviewed','reviewing'].includes(r.status);const canOutcome=['approved_for_processing','processing','action_required'].includes(r.status);return '<div class="hero"><div class="eyebrow">'+esc(r.requestId)+'</div><div class="row"><h2>'+esc(r.request.title)+'</h2><span class="pill" data-status="'+esc(r.status)+'">'+esc(r.status)+'</span></div><div class="meta-grid"><dl class="card"><dt>Requester</dt><dd>'+esc(r.requester.name)+'</dd><dd class="muted">'+esc(r.requester.email)+'</dd></dl><dl class="card"><dt>Organization</dt><dd>'+esc(r.requester.organization||'—')+'</dd></dl><dl class="card"><dt>Service</dt><dd>'+esc(r.request.serviceType)+'</dd></dl></div></div><section class="section"><h3>Request context</h3><p>'+esc(r.request.description)+'</p><h3>Desired outcome</h3><p>'+esc(r.request.desiredOutcome)+'</p></section><section class="section"><h3>Authority</h3><div class="control-grid"><dl class="card"><dt>Processing</dt><dd>'+String(r.controls.processingAuthorized?'AUTHORIZED':'GATED')+'</dd></dl><dl class="card"><dt>Publication</dt><dd>'+String(r.controls.publicationAuthorized?'AUTHORIZED':'GATED')+'</dd></dl><dl class="card"><dt>Provider egress</dt><dd>'+String(r.controls.externalProviderEgressAuthorized?'AUTHORIZED':'GATED')+'</dd></dl></div></section><section class="section"><h3>Evidence files</h3>'+(r.files.length?r.files.map(f=>'<div class="file"><div><strong>'+esc(f.name)+'</strong><small>'+esc(f.type)+' · '+humanBytes(f.size)+' · SHA-256 '+esc(f.sha256.slice(0,16))+'…</small></div><a class="btn" href="/api/requests/'+encodeURIComponent(r.requestId)+'/files/'+encodeURIComponent(f.fileId)+'">Download</a></div>').join(''):'<p>No files attached.</p>')+'</section><section class="section"><h3>Review decision</h3><div class="actions">'+(canReview?'<button class="btn" data-action="start-review">Start review</button>':'')+'</div>'+(canDecide?'<div class="decision"><textarea class="field" data-decision-note placeholder="Record the scope/rights decision and processing boundary."></textarea><button class="btn btn-primary" data-action="approve">Approve + dispatch</button><button class="btn btn-danger" data-action="reject">Reject</button></div>':'<p class="muted">Decision controls are available only before processing dispatch.</p>')+(r.review?'<div class="notice"><strong>'+esc(r.review.decision||r.review.state||'review')+'</strong> · '+esc(r.review.reviewerEmail||'')+'<br>'+esc(r.review.note||'')+'</div>':'')+'</section><section class="section"><h3>Workflow events</h3>'+(r.workflow?.events?.length?r.workflow.events.slice().reverse().map(eventHtml).join(''):'<p class="muted">No workflow events recorded yet.</p>')+'</section><section class="section"><h3>Outcome writer</h3>'+(canOutcome?'<form class="outcome-form" data-outcome-form><div class="split"><input class="field" name="headline" maxlength="180" required placeholder="Outcome headline"><select class="field" name="status"><option value="completed">Complete request</option><option value="action_required">Action required</option><option value="failed">Mark failed</option></select></div><textarea class="field" name="summary" maxlength="1200" required placeholder="Evidence-grounded outcome summary"></textarea><textarea class="field" name="nextAction" maxlength="480" placeholder="Next action"></textarea><input class="field" name="confidence" maxlength="80" placeholder="Confidence / review state"><button class="btn btn-primary" type="submit">Record outcome</button></form>':'<p class="muted">Outcome writing becomes available after processing authority is granted.</p>')+(r.workflow?.outcome?'<div class="notice"><strong>'+esc(r.workflow.outcome.headline||r.workflow.outcome.status||'Outcome')+'</strong><br>'+esc(r.workflow.outcome.summary||'')+'<br><span class="muted">'+esc(r.workflow.outcome.nextAction||'')+'</span></div>':'')+'</section>'}
async function openRequest(id){currentId=id;queueEl.querySelectorAll('[data-id]').forEach(x=>x.dataset.active=String(x.dataset.id===id));detailEl.innerHTML='<div class="empty">Loading request…</div>';try{const r=await api('/api/requests/'+encodeURIComponent(id));detailEl.innerHTML=detailHtml(r);detailEl.querySelector('[data-action="start-review"]')?.addEventListener('click',()=>mutate('review',{}));detailEl.querySelector('[data-action="approve"]')?.addEventListener('click',()=>decision('approve'));detailEl.querySelector('[data-action="reject"]')?.addEventListener('click',()=>decision('reject'));detailEl.querySelector('[data-outcome-form]')?.addEventListener('submit',writeOutcome)}catch(e){detailEl.innerHTML='<div class="empty">'+esc(e.message)+'</div>';notify(e.message,'error')}}
async function mutate(action,body){try{await api('/api/requests/'+encodeURIComponent(currentId)+'/'+action,{method:'POST',body:JSON.stringify(body)});notify('Request updated.');await loadQueue();await openRequest(currentId)}catch(e){notify(e.message,'error')}}
function decision(action){const note=detailEl.querySelector('[data-decision-note]')?.value.trim()||'';if(note.length<10){notify('Record at least 10 characters of decision context.','error');return}mutate(action,{note})}
async function writeOutcome(ev){ev.preventDefault();const f=new FormData(ev.currentTarget);await mutate('outcome',{status:f.get('status'),headline:f.get('headline'),summary:f.get('summary'),nextAction:f.get('nextAction'),confidence:f.get('confidence')})}
refreshBtn.addEventListener('click',loadQueue);loadQueue();setInterval(()=>{if(!document.hidden)loadQueue()},30000);
`;

export function reviewHtml(): string {
  return `<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta name="robots" content="noindex,nofollow"><title>TMG Work Review Console</title><link rel="stylesheet" href="/assets/review.css"></head><body><main class="shell"><header class="topbar"><div class="brand"><span class="mark">TMG</span><div><strong>Work Request Review Console</strong><small>Authenticated authority + workflow dispatch</small></div></div><div class="operator"><strong data-operator>Authenticated operator</strong><small>Cloudflare Access protected</small></div></header><section class="layout"><aside class="panel"><div class="panel-head"><h1>Review queue</h1><button class="btn" data-refresh>Refresh</button></div><div class="queue" data-queue><div class="empty">Loading queue…</div></div></aside><article class="panel detail" data-detail><div class="empty">Select a work request to inspect evidence and decide authority.</div></article></section></main><div class="toast" data-toast hidden></div><script type="module" src="/assets/review.js"></script></body></html>`;
}
