# Ruleset Quality Pass Canary

This temporary file exists only to prove that a clean pull request with `Quality / validate` passing remains mergeable under the `TMG Main Protection` ruleset.

It is not intended to merge and will be removed when the canary PR is closed.
